Requires pillow to be installed.

For Python 2 or earlier
---------------------------------
python -m pip install pillow


For Python 3 or newer
---------------------------------
py -m pip install pillow